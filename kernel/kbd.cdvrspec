# PS/2 keyboard driver — legacy, routed through the platform bus.
# IRQ line 1 on the IOAPIC; framework allocates the driver vector
# and wires the redirection entry.

driver "kbd" {
    match {
        bus: platform
        name: kbd
    }
    capabilities {
        irq: auto
    }
    lifecycle {
        on_irq: kbd.handler
    }
}
