build/
.caustic/
*.s
# ...but hand-written assembly sources (not compiler/assembler output)
# are real sources the build needs — keep them tracked.
!kernel/smp_asm.s
!kernel/syscall_entry.s
*.o
.caustic-src/
.claude-docs/
# superpowers skill scratch (plans/specs) — keep local, never track
docs/superpowers/
# Python bytecode caches (e.g. scripts/png2csif.py → scripts/__pycache__/)
__pycache__/
