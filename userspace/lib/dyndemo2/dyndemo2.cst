// dyndemo2.cst — a SECOND shared library, so the multi-library path is proven
// and not merely believed: two .csl in one program, each at its own base.
fn mul(a as i64, b as i64) as i64 { return a * b; }
