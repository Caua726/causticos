name "anim"
version "0.1.0"
author "Caua"

// Library. Consumers import it via:
//   use "../lib/anim/bezier.cst" as bez;    // easing curves (LUT)
//   use "../lib/anim/anim.cst" as anim;     // timeline pool over those curves
