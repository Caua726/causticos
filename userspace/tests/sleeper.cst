// sleeper — sleeps forever. A test fixture: spawned in the background so ps/top
// have a long-lived, named process to display.
use "../../../Caustic/std/os/causticos.cst" as sys;

fn main(argc as i64, argv as *u8) as i32 {
    while (1 == 1) { sys.sleep_ns(1000000000); }
    return 0;
}
