# shell — no window manager. /init is the shell, full-screen, and the tools are
# the same ones the desktop ships. Useful when the thing being debugged is
# underneath the WM and a compositor in the way is noise.

include base

init shell
